{
  "name": "sdd-extension",
  "version": "0.1.0",
  "description": "Spec-driven development (SDD) extension for Gemini CLI.",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.5.0",
    "@commitlint/config-conventional": "^20.5.0",
    "husky": "^9.1.7"
  },
  "scripts": {
    "prepare": "husky"
  }
}
